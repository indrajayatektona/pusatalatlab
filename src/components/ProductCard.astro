---
import type { CollectionEntry } from 'astro:content';

interface Props {
  product: CollectionEntry<'products'>;
}

const { product } = Astro.props;
const { title, id, category, image, standards } = product.data;

// --- LOGIC GAMBAR AMAN (FALLBACK) ---
// Jika 'image' ada isinya, pakai itu. 
// Jika kosong/undefined, pakai placeholder dari placehold.co
const imageSrc = image && image.trim() !== "" 
  ? image 
  : "https://placehold.co/400x400/e2e8f0/1e293b?text=No+Image";

// Logic Alt Text yang aman
const standardText = standards && standards.length > 0 ? `Standar ${standards[0]}` : '';
---

<a href={`/products/${product.slug}`} class="group relative flex flex-col bg-white border-2 border-civil-dark h-full shadow-hard-sm hover:shadow-none hover:translate-x-[2px] hover:translate-y-[2px] transition-all duration-200">
  
  <div class="relative w-full aspect-square bg-gray-100 overflow-hidden border-b-2 border-civil-dark">
    <img 
      src={imageSrc} 
      alt={`Jual ${title} ${standardText}`}
      class="w-full h-full object-cover object-center group-hover:scale-105 transition-transform duration-500"
      loading="lazy"
    />
    
    <div class="absolute top-2 left-2 bg-civil-secondary text-civil-primary text-[10px] font-black uppercase px-2 py-1 border border-civil-dark shadow-sm z-10">
      {category}
    </div>

    <div class="absolute bottom-0 right-0 bg-civil-dark text-white text-[10px] font-mono font-bold px-2 py-1 z-10">
      {id}
    </div>
  </div>

  <div class="p-4 flex flex-col flex-grow">
    
    {/* Cek apakah standards ada DAN punya isi */}
    {standards && standards.length > 0 && (
      <div class="flex flex-wrap gap-1 mb-2">
        {standards.slice(0, 2).map((std) => (
          <span class="inline-block bg-gray-100 text-gray-600 text-[10px] font-bold px-1.5 py-0.5 rounded border border-gray-300">
            {std}
          </span>
        ))}
      </div>
    )}

    <h3 class="text-sm md:text-base font-bold text-civil-primary leading-tight mb-2 group-hover:text-civil-secondary transition-colors line-clamp-2">
      {title}
    </h3>

    <div class="mt-auto pt-3 flex items-center justify-between border-t border-gray-100">
      <span class="text-[10px] font-bold text-gray-400 uppercase tracking-wider group-hover:text-civil-dark">
        Lihat Detail
      </span>
      <div class="text-civil-primary group-hover:translate-x-1 transition-transform">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
      </div>
    </div>
  </div>
</a>